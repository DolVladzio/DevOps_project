# == ConfigMap ===================================
apiVersion: v1
kind: ConfigMap
# ----------------------------
metadata:
  name: app-config
  namespace: default
# ----------------------------
data:
  ### BACKEND ENV ###
  DB_HOST: postgres
  DB_NAME: database
  DB_USER: db_user
  DB_PASSWORD: db_password
  # PostgreSQL env
  POSTGRES_USER: db_user
  POSTGRES_PASSWORD: db_password
  POSTGRES_DB: database
  # Redis env
  REDIS_HOST: redis
